<?php

return array(
    // AtansUser_Form_PermissionForm
    "The permission name already in use" => "The permission name already in use",

    // AtansUser_Form_RegisterForm
    "The username already taken" => "The username already taken",
    "The email already taken"    => "The email already taken",

    // AtansUser_Form_RoleForm
    "The role name already in use" => "The role name already in use",

    // AtansUser_Form_UserAddForm
    "The username already taken" => "The username already taken",
    "The email already taken"    => "The email already taken",
);