<?php
$title = $this->translate('權限管理');
$this->headTitle($title);

?>
<?php if ($flashMessages): ?>
    <div class="alert alert-info">
        <a class="close" data-dismiss="alert" href="#">&times;</a>
        <ul>
            <?php foreach ($flashMessages as $flashMessage): ?>
                <li><?php echo $this->escapeHtml($flashMessage) ?></li>
            <?php endforeach ?>
        </ul>
    </div>
<?php endif ?>

    <h3><?php echo $this->escapeHtml($title) ?></h3>

    <p>
        <a class="btn" href="<?php echo $this->url('zfcadmin/user/permission/add') ?>">
            <i class="icon-plus-sign"></i>
            <?php echo $this->translate('新增') ?>
        </a>
    </p>

<?php if (count($permissions) > 0) : ?>
    <table class="table table-bordered table-hover">
        <thead>
        <tr>
            <th class="span1">#</th>
            <th class="span5"><?php echo $this->translate('權限') ?></th>
            <th class="span5"><?php echo $this->translate('角色') ?></th>
            <th class="span1"><?php echo $this->translate('操作') ?></th>
        </tr>
        </thead>
        <tbody>
        <?php
        foreach ($permissions as $permission) {
            echo $this->partial('atans-user/permission/partial', array('permission' => $permission));
        }
        ?>
        </tbody>
    </table>
<?php else: ?>
    <div class="alert">
        <h4><?php echo $this->translate('找不到任何權限') ?></h4>
    </div>
<?php endif ?>